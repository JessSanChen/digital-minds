[
  {
    "name": "run_python",
    "description": "Execute a Python snippet in the operations sandbox and return stdout. The environment has the `requests` library available.",
    "input_schema": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "description": "Python source to execute."
        }
      },
      "required": ["code"]
    }
  }
]
